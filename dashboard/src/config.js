const config = {
  API_BASE_URL: 'https://inedited-illa-nidificational.ngrok-free.dev', // Replace with your PC's IP
  POLLING_INTERVAL: 30000, // 30 seconds
  RETRY_ATTEMPTS: 3,
  RETRY_DELAY: 1000,
};

export default config;